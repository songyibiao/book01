#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gitbook-plugin-include-codeblock/bin/include-codeblock.js" "$@"
  ret=$?
else 
  node  "$basedir/../gitbook-plugin-include-codeblock/bin/include-codeblock.js" "$@"
  ret=$?
fi
exit $ret
